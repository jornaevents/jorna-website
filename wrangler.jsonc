{
  // Cloudflare Pages project serving ./public (the marketing page at / and the
  // exported web app under /app). Migrated off Workers Static Assets, whose
  // many-file asset serving intermittently dropped the /app routes.
  //
  // Deploy with `npm run deploy` (build + `wrangler pages deploy` + verify).
  // book.jornaevents.com is attached to this project in the Cloudflare
  // dashboard (Pages → jorna-events → Custom domains) — moved here from the
  // apex jornaevents.com, which now belongs to the jorna-vendor project/repo.
  // The previous Worker "misty-water-0dbb" is retired.
  "name": "jorna-events",
  "pages_build_output_dir": "./public"
}
