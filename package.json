{
  "name": "jornaevents-site",
  "version": "1.0.0",
  "private": true,
  "description": "jornaevents.com — the marketing page plus the Jorna web app, served by Cloudflare Pages.",
  "scripts": {
    "install:app": "npm --prefix web install",
    "build": "npm --prefix web run build",
    "deploy": "node scripts/deploy.mjs",
    "deploy:once": "npm run build && wrangler pages deploy public --project-name jorna-events --branch main --commit-dirty=true"
  },
  "devDependencies": {
    "wrangler": "^4.112.0"
  },
  "allowScripts": {
    "esbuild@0.28.1": true,
    "fsevents@2.3.3": true,
    "sharp@0.34.5": true,
    "workerd@1.20260714.1": true
  }
}
